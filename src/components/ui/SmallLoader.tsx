import React from "react";

const SmallLoader: React.FC = () => {
	return (
		<div className="flex flex-col self-center items-center justify-center m-2">
			<div className="small-loader"></div>
		</div>
	);
};

export default SmallLoader;
